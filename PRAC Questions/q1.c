#include <stdio.h>

int main(void) {
    //int x = (1<<7) | (1<<5) | (1<<4) | (1<<2) | (1<<1);
    int x = (1<<5) | (1<<4) | (1<<3) | (1<<1);
    printf("Number: %d",x);
    return 0;
}

// ANSWER
// Variable x declared and assigned int value 182